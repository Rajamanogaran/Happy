# Docker Compose reads .env automatically. For python -m happy, export variables in your shell.
# Replace with a long unique password before sharing access. Never commit .env.
HAPPY_PASSWORD=
# Optional local Python storage override (Compose uses /data/happy.sqlite3):
# HAPPY_DB=/absolute/path/happy.sqlite3
