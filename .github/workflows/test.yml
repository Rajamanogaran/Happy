name: Test Happy
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: pip install -e .
      - run: python -m pytest -q
      - run: npm install --prefix .cache/ui-tests --no-audit --no-fund jsdom
      - run: NODE_PATH=.cache/ui-tests/node_modules node scripts/test_ui.cjs
      - run: node --check happy/static/app.js
